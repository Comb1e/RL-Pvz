# Promoted after differential tests and three repeated collection/update measurements.
# See docs/validation.md#gpu-performance. Existing explicit configurations retain their defaults.
simulator = "cuda"
device = "cuda"
n_envs = 128
rollout_steps_per_env = 128
